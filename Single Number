class Solution {
    public int singleNumber(int[] nums) {
        int num = 0;
        for(int index=0; index<nums.length;index++){
            num^=nums[index];
        }
       return num; 
    }
}
